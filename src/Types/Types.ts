export type User = {
    name: string;
    avatar: string;
  };

  export type TopBar = {
    title: string;
    
  };
  export type QuestionBox = {
    id: number;
    title: string;
    img: string;
    time: string;
    date: string;
    answerCount: number;
    
    
};